[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "rs-agent"
version = "0.1.0"
description = "Multi-agent remote-sensing image change understanding pipeline"
readme = "README.md"
requires-python = ">=3.9,<3.12"
dependencies = [
  "httpx>=0.27,<1",
  "openai>=1.30,<2",
  "pillow>=10,<12",
  "pydantic>=2.7,<3",
  "pydantic-settings>=2.2,<3",
  "python-dotenv>=1,<2",
  "pyyaml>=6,<7",
  "tenacity>=8.2,<10",
]

[project.scripts]
rs-agent-run = "rs_agent.applications.rs_pipeline:main"
rs-agent-cc = "rs_agent.applications.rs_cc:main"
rs-agent-vqa = "rs_agent.applications.rs_vqa:main"
rs-agent-batch = "rs_agent.applications.rs_batch:main"
rs-agent-preflight = "rs_agent.applications.preflight:main"
rs-agent-export = "rs_agent.applications.export_results:main"
rs-agent-web = "rs_agent.applications.web_demo:main"

[project.optional-dependencies]
evaluation = [
  "numpy>=1.24,<2",
  "openpyxl>=3.1,<4",
  "pandas>=2,<3",
]
workflow = ["langgraph>=0.2,<1"]
web = ["streamlit>=1.50,<2"]
dev = [
  "pytest>=8,<9",
  "ruff>=0.5,<1",
]

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.ruff]
line-length = 100
target-version = "py39"
extend-exclude = ["legacy"]

[tool.ruff.lint]
select = ["E4", "E7", "E9", "F", "I"]
